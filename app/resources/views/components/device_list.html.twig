<div class="space-y-4">
    <div class="w-full bg-white border border-gray-200 rounded-xl p-4 space-y-3">
        <h2 class="text-sm font-semibold text-gray-700 tracking-wide">{{ connected_devices_count }} appareils connectés</h2>
        <div class="flex flex-col gap-3 text-sm text-gray-800 max-h-64 overflow-y-auto">
            {% for connected_device in connected_devices %}
                {% include "components/device.html.twig" with {
                    device: connected_device,
                    current_ip: device.ipAddress,
                    current_mac: device.macAddress
                } only %}
            {% endfor %}
        </div>
    </div>
    <p class="text-gray-500 text-xs text-center">Les appareils connectés à ce réseau ne peuvent pas communiquer entre eux pour des raisons de sécurité.</p>
</div>
